name: Deploy VeilTalk Privacy Policy

on:
  push:
    branches: [main]
    paths:
      - 'docs/veiltalk/**'
      - '.github/workflows/veiltalk-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: veiltalk-pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload VeilTalk site
        uses: actions/upload-pages-artifact@v4
        with:
          path: docs/veiltalk

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
