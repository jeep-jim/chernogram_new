name: chernogram
description: Chernogram chat-only stability test
publish_to: 'none'
version: 0.23.1+56

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  app_links: ^7.0.0
  cryptography: ^2.9.0
  http: ^1.5.0
  ota_update: ^7.1.0
  package_info_plus: ^8.3.1
  share_plus: ^10.1.4
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
