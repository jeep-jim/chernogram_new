name: svet
description: SVET — direct local file transfer between your devices
publish_to: 'none'
version: 0.80.0+80

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  desktop_drop: ^0.7.1
  file_picker: 11.0.2
  path_provider: ^2.1.6
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
