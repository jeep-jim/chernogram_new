name: cernogram_realtime_gateway
description: Encrypted JSON-only realtime gateway for Cernogram.
publish_to: none
version: 0.1.0

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  crypto: ^3.0.6

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.15
