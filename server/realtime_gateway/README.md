# Cernogram Realtime Gateway

Encrypted WebSocket transport for Cernogram. The gateway routes opaque ciphertext and never receives room keys or plaintext.

This directory will contain the standalone Dart service, Docker image and protocol tests. Client integration remains behind a feature flag until Android and Windows recovery tests pass.
